"""Replacing the running app with a newer copy of itself.

`signature` verifies the payload, `key` holds the public key, `install` unpacks and stages and hands
the swap to update_helper.c. See docs/specs/2026-09-19-issue-58-update-design.md.
"""
