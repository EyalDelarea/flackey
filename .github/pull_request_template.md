<!--
Label this PR one of: enhancement, bug, documentation, chore, ignore-for-release.
See docs/RELEASING.md.

If it's enhancement, bug, or documentation: the release notes pull the PR TITLE
verbatim into the changelog. Title it the way a user reading "what's new" would
want to read it -- what changed for them, not the internal mechanism or file
names. "Fix downloads silently failing when Soulseek disconnects" reads well in
release notes; "Fix race in retry queue" doesn't.

If this PR is internal only (CI, deps, refactor, docs-for-contributors) and
shouldn't appear in release notes at all, label it ignore-for-release instead
of chore.
-->

## Summary

## Test plan
